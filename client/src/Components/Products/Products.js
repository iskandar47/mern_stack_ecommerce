import React from 'react'
import { Container, Typography } from '@material-ui/core'

function Products() {
    return (
        <Container maxWidth='xl'>
            <Typography variant="h1" color="secondary">Products Page</Typography>
            <Typography variant="body1" color="primary">Products Page</Typography>
        </Container>
    )
}

export default Products
